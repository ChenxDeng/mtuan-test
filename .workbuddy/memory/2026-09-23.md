# 2026-09-23

- 完成「写下烦恼」演示流程（index.html / styles.css / app.js）：点信纸 → 右 1/3 手绘信纸面板（引导语「把一天的烦心事写下来飞走吧」，不存储）→ 对折两次（3D rotateX 上下两半）→ 变纸飞机（复用 parts/city-plane-right.png，机头固有偏角 28°）→ 弹簧跟随鼠标 → 窗台固定锚点 (0.475, 0.70) 发出三条虚线轨迹（终点为窗景天空三处）→ 点击轨迹后飞机先汇入窗台起点再沿轨迹飞出、渐小渐隐 → 回声「烦恼已经飞出窗外。」+「再写一张」重置。
- 关键设计决策：轨迹锚定窗台而非跟随飞机（否则轨迹追着鼠标跑、永远点不到）；hit 区域用 dasharray 跳过前 28% 汇聚段避免三线歧义。
- 验证：scripts/check-letter-flow.cjs（复用 codex-runtime 的 Playwright + 本机 Chrome for Testing），12 项全过；截图在 output/preview/flow-*.png。
- 教训：同一文件的多个 Edit 不要放在同一批并行调用里，会静默丢失部分修改。

## 第二轮迭代（10:57 反馈）

- 移除「收起」按钮、轨迹虚线、回声文案（用户嫌 OPEN WINDOW 下面放字丑）；「折起来」按钮去边框；对折改为一次。
- 中文全部改行楷：font stack `'Xingkai SC','STXingkai','Xingkai','Kaiti SC','STKaiti','Songti SC',serif`（macOS 确认有 Xingkai SC）。
- 投放交互改为呼吸区：窗内右下天空 (left 53.2% top 32.5%) 虚线圈 + 手绘箭头 + 小字「拖动到这里扔飞机」，飞机拖入停留 300ms 自动扔出（拖离取消）。
- 飞行动效：S 型摆线（主方向指向街道灭点 0.502,0.232，垂直向正弦摆动 1.1 周期、幅度衰减），ease-out 先快后缓，scale 1→0.16 透视缩小，尾段渐隐。
- 机头正立修复（关键）：单一俯视图 sprite 旋转超 ±90° 会肚皮朝天。解法 = 角度先归一化到 (-180,180]，超 ±90° 时改用 `scaleX(-1)` 镜像 + 减 180° 旋转；且朝向取「指向鼠标方向」而非弹簧速度（速度在过冲时乱摆），距离鼠标 <80px 时冻结朝向。

## 第三轮迭代（11:21 反馈）

- 字体分工（项目约定）：仅信纸内容行楷（Xingkai SC），其余中文 UI 全部苹方（PingFang SC）。
- 投放区移到窗户右下角 (left 62.5% top 61.5%)，去掉虚线描边圆，改为 ::before 径向渐变 + blur(8px) 弥散阴影呼吸；文字 translateX(-32%) 避免被右窗框裁切。
- 飞行轨迹按用户手绘图实现：Catmull-Rom 平滑路径穿过路径点（scene 分数坐标，存于 app.js WAYPOINTS）——右下出发 → 左掠街道 → 左侧回旋 → 右上没入远楼；ease-out 先快后缓，smoothstep(0.18,1) 段内 scale 1→0.24 透视缩小，尾部 15% 渐隐。
- 排查花絮：Playwright 截图中「飞机消失」实为截图时刻晚于飞行结束（arm 在 mouse.move 过程中即触发），用同步记录 transform+截图定位，渲染本身正常。

## 第四轮迭代（11:44 反馈）

- 投掷改为手动：停进投放区即 armed（不再自动计时），pointerdown 点击才扔；提示语改为「拖动到这里，点击扔出」。
- 飞机在投放区内自动摆正：ease 到 READY_HEADING=165°（与出手方向一致），不再保持跟随朝向。
- 修复镜像角度映射 bug：scaleX(-1) 后机头基准角是 180-28=152°，渲染时应二选一取倾斜更小的方案（ru = heading-28，rf = heading-152，取 |r| 小者 + 是否镜像），之前镜像分支会让机头偏 56°。
- zone 提示语加浅色晕影（多层 cream 色 text-shadow）保证在风景上可读。
- OPEN/WINDOW 行距 2.65vw→1.32vw，整体上移约半行（top 37.8%→32.5%）。

## 第五轮迭代（12:03 反馈）

- 待发朝向改为机头指向页面左上方：READY_HEADING=207°；WAYPOINTS 首点改为向左上抬起 (0.58,0.575)，使出手切线与待发朝向一致、无朝向跳变。
- 去掉投放区的箭头 svg 和 is-armed 呼吸加速；is-armed 仅轻微加深弥散阴影。
- 提示语移到投放区下沿（justify-content:flex-end + translate(-32%,40%)），避免被停驻的飞机遮挡。

## 第六轮迭代（12:12 反馈）

- 提示语改两行「拖动到这里 / 点击扔出」，投放区正中间显示；飞机停驻（is-armed）时文字淡出（200ms），避免与飞机重叠。

## 第七轮迭代（12:17 反馈）

- 补全粉/蓝飞机点击：openDesk(mode) 三态——write（写信）、reply（我的回信：.paper-read 只读案例，日期右对齐 + 原文 + 虚线分隔「匿名回复：加油…」）、share（别人的烦恼：.paper-share 上半只读原文 + 虚线分隔下半 textarea，placeholder「鼓励一下TA吧～」）。Esc 一律关闭（此前焦点在 textarea 时 Esc 失效导致测试卡死）。
- 用户要求：过程截图不要保存进项目（已清空 output/preview/*.png/*.json），验证脚本 check-letter-flow.cjs 改为纯断言输出；自查截图一律存 /tmp。
- 字体约定重申：信纸内容行楷，UI 苹方。

## 第八轮迭代（12:35 反馈）

- .letter-reply 补上行楷 font（此前继承浏览器默认字体）。
- 退出交互：window click 监听——面板打开时点击 .desk 以外且非 .keepsake 的区域即 closeDesk；点另一架飞机直接切换面板（openDesk 增加面板态间切换，PANEL_MODES 映射）。

## 第九轮迭代（12:38 反馈）

- 交互提示动效：给每个 keepsake 的弹簧系统加 nudge() 一次性脉冲（sin 包络 1.7s 抬起-落下，注入 tick 的非激活 target），nudges 映射表暴露。
- 进场：pink/blue/letter 依次 500/800/1100ms 抬放一次。
- 闲置提醒：pointermove/pointerdown/keydown 记 lastActivity，setInterval(10s) 检查闲置≥10s 且 deskIdle()（desk 和 launch-zone 都 hidden）时 letter nudge 一次；hover 中/减少动效模式不触发。
- 坑：Playwright 探针的采样时刻与页面相对时间有偏移（networkidle 等待），先用页面内 console 逐秒采样确认逻辑正确。
